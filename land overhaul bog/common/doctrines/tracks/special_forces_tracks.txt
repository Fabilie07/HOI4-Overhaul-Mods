special_forces_marines = {
    name = DOCTRINE_TRACK_MARINES
    background = "GFX_marine_bg"
    icon = "GFX_doctrine_milestone_special_forces"
    icon_frame = "GFX_doctrine_decor_special_forces"

	active = {
		has_tech = marines
	}
    # SF mastery is sourced per-subdoctrine (each doctrine levels only from its own unit type); the subdoctrine override replaces these track conditions, so no track multiplier needs to be set here, but for the training and banking purposes we are going to plaster multiplier here as well.
    mastery = {
        multiplier = 6
        categories = {
            category_marines_and_amphibious
        }
    }
}

special_forces_paratroopers = {
    name = DOCTRINE_TRACK_PARATROOPERS
    background = "GFX_paratroopers_bg"
    icon = "GFX_doctrine_milestone_special_forces"
    icon_frame = "GFX_doctrine_decor_special_forces"

	active = {
		has_tech = paratroopers
	}
     # SF mastery is sourced per-subdoctrine (each doctrine levels only from its own unit type); the subdoctrine override replaces these track conditions, so no track multiplier needs to be set here, but for the training and banking purposes we are going to plaster multiplier here as well.
    mastery = {
        multiplier = 6
        categories = {
            category_paratroopers
        }
    }
}

special_forces_mountaineers = {
    name = DOCTRINE_TRACK_MOUNTAINEERS
    background = "GFX_mountaineers_bg"
    icon = "GFX_doctrine_milestone_special_forces"
    icon_frame = "GFX_doctrine_decor_special_forces"

	active = {
		has_tech = tech_mountaineers
	}
    # SF mastery is sourced per-subdoctrine (each doctrine levels only from its own unit type); the subdoctrine override replaces these track conditions, so no track multiplier needs to be set here, but for the training and banking purposes we are going to plaster multiplier here as well.
    mastery = {
        multiplier = 6
        categories = {
            category_mountaineers
        }
    }
}

special_forces_rangers = {
    name = DOCTRINE_TRACK_RANGERS
    background = "GFX_rangers_bg"
    icon = "GFX_doctrine_milestone_special_forces"
    icon_frame = "GFX_doctrine_decor_special_forces"
	
	active = {
		has_tech = rangers_tech
	}
     # SF mastery is sourced per-subdoctrine (each doctrine levels only from its own unit type); the subdoctrine override replaces these track conditions, so no track multiplier needs to be set here, but for the training and banking purposes we are going to plaster multiplier here as well.
    mastery = {
        multiplier = 6
        categories = {
            category_rangers
        }
    }
}
